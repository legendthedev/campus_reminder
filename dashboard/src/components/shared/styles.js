export const btnStyle = (bg, small) => ({
  background: bg, color: '#fff', border: 'none', borderRadius: 7,
  cursor: 'pointer', padding: small ? '5px 12px' : '9px 20px',
  fontWeight: 600, fontSize: small ? 12 : 14,
});
